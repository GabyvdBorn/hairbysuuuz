<!DOCTYPE html>
<html>
  <head>
    <title>Hair by Suuz</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>
  <body>

        <% if current_page?(root_path) %>
                  <div class="nav-bar-white">
                    <%= link_to prijzen_path, class: "navbar-title" do %>
                          <p>Prijzen</p>
                        <% end %>
                      <%= link_to contact_path, class: "navbar-title" do %>
                        <p>Afpraak</p>
                    <% end %>
                  </div>
             <% else %>
                  <div class="nav-bar-black">
                      <%= link_to root_path, class: "navbar-title" do %>
                        <p>terug</p>
                      <% end %>
                        <%= link_to prijzen_path, class: "navbar-title" do %>
                          <p>Prijzen</p>
                        <% end %>
                  </div>
              <% end %>
      <div class="page-container">
        <%= yield %>
      </div>

    <% unless current_page?(root_path) %>
      <footer class="bg-light text-center text-lg-start">
      <div class="text-center p-2" style="background-color: transparant; color: #fcfcfc8f; letter-spacing: 4px; font-size: 10px; font-family: 'Work Sans', sans-serif;">
        © 2022 - DifferentEye
      </div>
    </footer>
    <% end %>


  </body>
</html>
